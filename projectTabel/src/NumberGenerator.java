public class NumberGenerator {
    public void nummers(){
        double x = Math.random();
        int[] tableContent = new int[100000];
        for (int i = 0; i >= 100000; i++){
            //double x = Math.random();
            int y = (int) (1 + Math.random());
            //
        }
    }
}
